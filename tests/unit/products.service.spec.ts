import {ProductService} from './../../src/services/products.service';

describe('ProductService', () => {
  it('getAllProducts makes a fetch call', () => {
    // TODO: mock fetch or something
  });
});
